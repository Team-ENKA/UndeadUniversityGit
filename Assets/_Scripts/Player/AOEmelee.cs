using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AOEmelee : MonoBehaviour
{

    /*
     * public Collider hitBox
     * public cooldown half a second
     * public bool isRunning (animation)
     * 
     * new script
     * hitBox is the circle around the player
     * find hitBox
     * activate isRunning, then turn it off again
     * look for enemy tags in area, deal 10 damage to it
     * if this kills the enemy, make dead student sprite
     * destroy a random pickup in the scene (healthbox or ammo)
     * 
     */
}
